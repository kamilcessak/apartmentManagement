export type TenantType = {
  _id: string;
  firstName: string;
  lastName: string;
  address: string;
  // apartmentId: string;
  invitationCode: string;
  isActive: boolean;
  owner: string;
  // rentAmount: string;
  // rentDueDate: string;
  email: string;
  phoneNumber: string;
};
